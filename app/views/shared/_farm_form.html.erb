<%= form_for @farm do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.label :farm_name %><br />
    <%= f.text_field :farm_name %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
